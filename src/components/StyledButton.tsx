import styled from "styled-components";

export const StyledButton = styled.button`
  padding: 10px;
  border-radius: 5px;
  background-color: white;
  transition: all 0.2s;

  &:hover {
    transform: scale(1.1);
  }
`;
